# Portfolio Builder AI App

Start backend: cd backend && npm install && npm start
Then open frontend/index.html and click Generate (backend mocks OpenAI responses). .env.example for OpenAI keys can be added for real integration.
